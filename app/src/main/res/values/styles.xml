<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Nav drawer style to set width specified by Material Design specification -->
    <style name="NavDrawer">
        <item name="android:layout_width">@dimen/nav_drawyer_width</item>
        <item name="android:layout_marginRight">@dimen/nav_drawer_margin_offset</item>
    </style>

    <style name="NavDrawerTextStyle" parent="Base.TextAppearance.AppCompat">
        <item name="android:textColor">@color/menu_item_selector</item>
        <item name="android:textSize">@dimen/navigationTextSize</item>
        <item name="android:tint">@color/menu_item_selector</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:background">@color/redColor</item>
        <item name="android:textColor">@color/whiteColor</item>
        <item name="android:textSize">@dimen/loginTextSize</item>
    </style>

    <style name="GreyBackGroundStyle">
        <item name="android:background">@color/dullGray</item>
    </style>

    <style name="tabTextSyle">
        <item name="android:textColor">@color/blackColor</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="loginButtonStyle">@style/ButtonStyle</item>
        <item name="greyBackGroundStyle">@style/GreyBackGroundStyle</item>
        <!--<item name="drawerArrowStyle">@style/DrawerArrowStyle</item>-->
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="customTabStyle" parent="Widget.Design.TabLayout">
        <!--<item name="tabPaddingStart">12dp</item>-->
        <!--<item name="tabPaddingEnd">12dp</item>-->
        <item name="tabIndicatorColor">@color/colorPrimary</item>
        <item name="tabIndicatorHeight">5dp</item>
        <item name="android:background">@color/whiteColor</item>
        <!--Tab Text Selection-->
        <item name="tabTextAppearance">@style/customTabText</item>
        <item name="tabSelectedTextColor">@color/colorPrimary</item>
    </style>

    <style name="customTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/tabTextSize</item>
        <item name="android:textColor">@color/blackColor</item>
        <item name="textAllCaps">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
